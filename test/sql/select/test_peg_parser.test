
statement ok
create table foo (a bigint);


# reserved keyword group
statement error
create table group (a bigint);
----
Parser Error: create table group (a bigint);: Error on line 1:20 -> syntax error, unexpected '(', expecting <PlainIdentifier>.

statement ok
select a from foo;

statement ok
select a from foo f;

statement ok
select f.a from foo f;

statement ok
select 1 as a;

statement ok
select f.a as a from foo f;

statement ok
insert into foo (a) select 1;

query I
select a from foo;
----
1

#query I
#select a from foo where a = 1;
#----
#1
#
#
#query I
#select a from foo where a > 1;
#----

query I
select sum(1+2)
----
3


statement ok
SELECT
    l_returnflag,
    l_linestatus,
    sum(l_quantity) AS sum_qty,
    sum(l_extendedprice) AS sum_base_price,
    sum(l_extendedprice * (1 - l_discount)) AS sum_disc_price,
    sum(l_extendedprice * (1 - l_discount) * (1 + l_tax)) AS sum_charge,
    avg(l_quantity) AS avg_qty,
    avg(l_extendedprice) AS avg_price,
    avg(l_discount) AS avg_disc,
    count(*) AS count_order
FROM
    lineitem
WHERE
    l_shipdate <= CAST('1998-09-02' AS date)
GROUP BY
    l_returnflag,
    l_linestatus
ORDER BY
    l_returnflag,
    l_linestatus;
