# name: test/sql/peg_parser/transformer/call.test
# description: 
# group: [transformer]

require autocomplete

require tpch

statement ok
call peg_parser();

# statement error
# call peg_parser();
# ----
# Binder Error: Cannot install PEGParser: a parser override is already active.
#
# statement ok
# CALL pragma_table_info('pg_am');
#
# statement error
# call peg_parser(on_error:='throw', enable_logging:=true);
# ----
# Invalid Input Error: Please execute "PRAGMA enable_logging" first.

statement ok
CALL dbgen(sf=0.003);

query IIIIIIIIIIIIIIII
from lineitem;
----

