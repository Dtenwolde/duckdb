{
  "description": "Test PEG Parser + transformer",
  "skip_compiled": "true",
  "on_init": "pragma enable_logging;SET logging_storage='stdout';call peg_parser(on_error='continue', enable_logging=true);",
  "statically_loaded_extensions": [
    "core_functions",
    "autocomplete"
  ]
}